# Slim runtime requirements for the streaming containers.
# Kept separate from the dev/dbt requirements.txt so the container layer
# rebuilds cheaply.
confluent-kafka~=2.5.0
boto3~=1.35.0
botocore~=1.35.0
pyarrow~=17.0.0
requests~=2.32.0
tenacity~=9.0.0
pyyaml~=6.0.0
