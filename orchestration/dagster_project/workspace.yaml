# Dagster workspace — points the webserver/daemon at the package's Definitions.
# Loaded by `make dagster-up` (which also sets DAGSTER_HOME).
load_from:
  - python_module:
      module_name: dagster_project
      working_directory: orchestration/dagster_project
      location_name: flightpulse
