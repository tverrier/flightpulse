# =============================================================================
# FlightPulse Python dependencies
# Source: SETUP.md § 2 Python Project
# Pinned to compatible-release (~=) for reproducibility while allowing patch
# updates. Bump deliberately via PR.
# =============================================================================

# --- dbt (warehouse adapter) ---
dbt-core~=1.8.0
dbt-snowflake~=1.8.0

# --- Orchestration ---
dagster~=1.8.0
dagster-dbt~=0.24.0
dagster-aws~=0.24.0
dagster-webserver~=1.8.0

# --- Streaming ---
confluent-kafka~=2.5.0

# --- Lakehouse / table format ---
pyiceberg[s3,glue]~=0.7.0

# --- Snowflake client ---
snowflake-connector-python[pandas]~=3.12.0

# --- Serving layer ---
streamlit~=1.38.0
fastapi~=0.115.0
uvicorn[standard]~=0.30.0
pydeck~=0.9.0

# --- ML model for /predict ---
lightgbm~=4.5.0
scikit-learn~=1.5.0
joblib~=1.4.0

# --- Observability ---
elementary-data[snowflake]~=0.16.0

# --- AWS SDK + utilities ---
boto3~=1.35.0
botocore~=1.35.0

# --- Data wrangling ---
pandas~=2.2.0
pyarrow~=17.0.0
numpy~=1.26.0

# --- Misc ---
pyyaml~=6.0.0
requests~=2.32.0
python-dotenv~=1.0.0
tenacity>=8.2,<10  # pyiceberg 0.7 needs tenacity<9
click~=8.1.0

# --- dev / test ---
pytest~=8.3.0
pytest-mock~=3.14.0
pytest-cov~=5.0.0
httpx~=0.27.0
ruff~=0.6.0
mypy~=1.11.0
